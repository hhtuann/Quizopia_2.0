# Legacy reference

The canonical legacy reference is:

```text
docs/reference/legacy-analysis.md
```

That file describes Quizopia 1.x and is **reference only**.

Quizopia 2.0 accepted product specifications and ADRs take precedence.

Context Pack v0.2 intentionally does not bundle `legacy-analysis.md` because the project repository already contains the full source-specific analysis. Do not recreate a condensed duplicate such as `legacy-summary.md`; duplicate reference documents can drift.
